/**
 * Problems
 * 69. Sqrt(x)
 * https://leetcode.com/problems/sqrtx/
 * @param {number} x
 * @returns {number}
 * Runtime: 88 ms (faster than 88.98 %)
 * Memory Usage: 40.1 MB (less than 55.81 %)
 */

const mySqrt = x => {
    return parseInt(Math.sqrt(x));
}